name: Test "Archive feature branch"

on: push

permissions:
  contents: write

jobs:
  call-archive-workflow:
    uses: ./.github/workflows/archive-feature-branch.yml
    with:
      git-branch: ${{ github.ref_name }}
      