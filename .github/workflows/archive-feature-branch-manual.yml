name: Archive feature branch (manual)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  call-archive-workflow:
    uses: ./archive-feature-branch.yml
    with:
      git-branch: ${{ github.ref_name }}
      